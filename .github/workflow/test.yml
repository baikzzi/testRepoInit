name: Notify Microsoft 365
on: [push]
jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Send notification to Microsoft 365
        run: |
          curl -H "Content-Type: application/json" -d "{ 'text': 'new push!!' }" ${{secrets.M365_WEBHOOK_URL}}
